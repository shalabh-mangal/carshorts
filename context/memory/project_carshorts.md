---
name: project-carshorts
description: "carshorts YouTube factory — current state, architecture, and the agreed system roadmap."
metadata: 
  node_type: memory
  type: project
  originSessionId: f62839e4-65b7-407f-a5e2-f8ee7b449445
  modified: 2026-07-21T16:27:36.612Z
---

Repo: `/Users/apple/Personal Projects/carshorts` (github.com/shalabh-mangal/carshorts, private). Channel: **carshorts** (3 Shorts live as of 2026-07-21: Nexon, Swift, Thar `mPbjAt4kSoE`).

**Pipeline (all one-command):** specs (Wikipedia gen-scoped + CarDekho manual lookup → specs_top5/ + specs_extras/ with news) → writescript (variants→judge→editor, personas deadpan/hype, formats, learnings injected) → produce (phrase-synced cuts via TTS word marks, speech-timed overlays, mood-matched music, ducked audio −14 LUFS, unified grade, QA gate with auto-fix loop + failure journal) → publishkit (title-promise check) → publish (uploads + links video_id to recipe) → analyze.py (metrics → learnings). QA = 12 checks; every render writes manifest + recipe card.

**Hard rules learned:** visuals must track narration phrase-by-phrase (user's #1 quality bar); vet every asset visually (Pexels tags lie — Jimnys tagged as Thar); blur/exclude number plates; never use third-party watermarked/ripped content (refused repeatedly — non-negotiable); edges of video must show the subject car; free tools for drafts, ElevenLabs (voice Charlie `IKne3meq5aSn9XLyUdCD` for Thar) only for finals with cached audio.

**Roadmap status (2026-07-21):** DONE: visual QA (vqa.py), orchestrator (pipeline.py draft→queue→approve→upload), retention-curve→beat mapping, inbox auto-ingest (ingest.py), .env + provider fallback chain. Analyst ran first live cycle (Thar, 24 views). DONE ALSO: hardening (golden tests + CI green), experiment calendar (pipeline --next), comment mining (comments.py), housekeeping (specs/ canonical, docs rewritten). IN PROGRESS: brain.py v1 (embedded judgment: triage/vet/news/strategy + data/brain_log.jsonl; Groq bounded + headless-claude hard calls) and portal.py v3 (localhost:8787 review station: beat-seek player, 12 issue + 4 win chips, IN-PORTAL SCRIPT EDITING (pencil → save & re-render via POST /api/script), render lock (<out>.lock written by produce; portal shows 'rendering…' + blocks actions), cache-busted player, auto-rework worker rework.py with live progress + QA-aware notes; beat_wins folded as keep-doing lessons). NEXT: semantic asset index, Hinglish re-issues, daily heartbeat cron.

**Pending user actions:** Thar 12-shot footage (assets/SHOT_CHECKLIST.md); rotate exposed keys (Groq/Gemini/Pexels/ElevenLabs); run analyze.py once Thar has 24-48h of data. ElevenLabs ~4.5k credits left.

**Not-listening incident (2026-07-22):** owner tagged 'text on screen' on all beats 3x + notes said remove — system only folded to learnings (feeds future scripts), re-rendered identical video 3x. Fixes: rework._render_directives() maps tags/notes → produce flags persisted as card render_flags (--no-kwcaps, --music=none); --no-kwcaps had a latent crash (None keyword_span in manifest) — never survived a run before, golden test added; punch-up rewrites number-guarded (one fabricated '$40,000' for Indian market — auto-reverted now); learnings pruned of contradictory overlay lessons. RULE: every feedback tag must map to a render-changing action, or the owner perceives deafness.

**Text overlay engine (2026-07-22):** word-synced highlight pops — single engine, replaces keyword+callouts (callouts' fuzzy anchoring was the mistiming). produce._word_pops(): script-curated ScriptSegment.pops fragments + auto spec figures, matched word-exact vs TTS marks; no match = no text; max 2/beat, 0.6s gap. Renderer: 84px yellow(figures)/white, accent bar, scale-in, h*0.64. QA check 13 (pops uncrowded/inside). _exact_span fix: strip sentence-final periods. Verified frame-level: text mid-pop, none between. Thar draft 8 pops 13/13 green awaiting review.

**Overlay style v2 (research-driven, 2026-07-22):** 4-agent research (Hormozi/MrBeast/carwow/Donut/safe-areas/kinetic) → yellow RETIRED (clip-farm signal); white base + single cyan accent #7EE5E3 on figures; Montserrat Black (assets/fonts, OFL); stroke 9% of font, blurred shadow. Pop kinds: word(96px white), number(cyan digits+white unit+0.35s marker-wipe), reaction(110px y0.30 slam, fires in silence AFTER punchline, straddles cuts, exempt from rail gap), card(count-up autofit, 1/short, payoff stat). Entrance 0.92→1.05→1.00/0.22s. Cap 3/beat; spec values auto-candidates; writer prompt emits pops. Full spec in workflow output tasks/wqnaa4pqg.output (session f62839e4).

**Rework brain (2026-07-22):** 3rd deafness incident — free-text notes only fed learnings (future scripts), re-render identical. Fix: rework._notes_to_actions() LLM→structured actions (add_pops verbatim-validated, set_flag whitelisted) applied to THIS render; honest no-op skip (no actionable signal → skip re-render, card says why). Also _exact_span: edge-tts multi-word marks ('Level 2' = one mark) flattened to word granularity — was silently killing pops. RULE affirmed: every feedback channel must either change the render or explicitly say it can't.

**Karaoke density (2026-07-22):** owner wanted every listed overlay → select-all-then-trim in _word_pops (match all, sort by time, trim rail pop to next start, floor 0.5s, cap 6/beat, gap 66ms); card+reaction = floaters (own slots, exempt from rail chain; rail cleared during card window, card-figure dedupe). Card anchor lesson: possessives break word-exact ('Smart money' vs "money's") — anchor on exact spoken tokens. CTA like/share/subscribe = reaction pop (words not narrated). Thar draft: 20 pops.

**FIRST FULL-LOOP PUBLISH (2026-07-22):** Thar approved 5/5 after 7 review rounds → ElevenLabs final → YouTube video_id EXCHHyUDyyg (youtube.com/shorts/EXCHHyUDyyg). Approved house style folded to learnings (karaoke pops/cyan/count-up card/reactions). NEXT: analyze.py after 24-48h (retention curve → beats), then video #4 Creta via pipeline --next (calendar slot 1).

**Two-stage approval (2026-07-22):** draft approve → ElevenLabs final + VQA → card status final_review (portal plays FINAL, purple pill, Publish button) → pipeline --publish uploads. Upload failure → back to final_review with retry note. Portal spawns both stages detached.

**Agentic north star (2026-07-22, owner directive):** system gets own Claude brain via subscription (explicitly authorized). Built P1: src/carshorts/agent.py harness (claude -p, budget 12/day in data/agent_budget.json, 40-turn cap, journal data/agent_log.jsonl), agents/mechanic.md + supervisor.md charters, rework dead-ends escalate to mechanic (can edit code/re-render/verify; hard rules: no .env, no paid APIs, no uploads, tests green), data/brain_inbox.jsonl = menu-growth proposals. Smoke run OK (~$0.39). Remaining: P2 scriptwright (news crawl+humor+pops), P3 analyst (competitor+analytics research), P4 composer (car personality→music), P5 curator (asset crawl+stitching), P6 supervisor daily cadence. Claude role = supervisor: audit escalations, grow free menu, correct course.

**Mechanic ops lessons (2026-07-22):** (1) right-size tasks — one concern per run; 'trim script + refactor pools + render' blew the 40-turn cap (raised to 60). Run 1 still delivered the two-tier stock pool fix (assets/cars/<slug>/stock/ car-scoped + assets/stock/ generic) — committed. (2) P2 scriptwright live-validated on Creta: real sourced news + funny script + guards clean; its render exposed Thar-hardcoded QA/edge-beat family checks — now car-agnostic via manifest subject_families + extras aliases.

**Full brain roster + cadence (2026-07-22):** P1-P5 done+validated. P6 = scheduled task 'carshorts-supervisor-audit' every 8h (LOCAL app scheduler — runs while Claude app open; true cloud-independent = later upgrade). Cadence: scriptwright 1x/video, curator auto when pool<12, composer 1x/car (cached sound_profiles/), analyst 1x/day, mechanic on-failure only, supervisor every 8h + on-demand; budget 12 runs/day. RULE: new analyst findings integrate into the CURRENT queued video same-day (Creta got AGAIN. trigger + 1 OR 2? poll from sweep 1). Word ceiling ~112 for deadpan voice.

**Supervision pattern proven (2026-07-22 eve):** feedback round = free menu handles repeats; mechanic gets precise orders for engine work (card holds till beat end; lss pop kind auto-fires when CTA speaks like/share/subscribe → procedural icon strip); supervisor does taste passes the agents can't (icon art redraw: white silhouette + MaxFilter dilated outline + labels). Owner overrides analyst tactics (1-or-2 poll rejected). Reaction pops must be self-explanatory or die ('Zero memory.' removed). Creta draft: all 6 feedback items applied, QA green, awaiting review.

**Standing mandate (owner, 2026-07-22): continuously self-improve WITHOUT being asked.** Operationalized: 8h scheduled supervisor ships 1 improvement/shift (duty 5 of carshorts-supervisor-audit task); interactive sessions should proactively fix/improve too. Tier ladder in ROADMAP.md (T1 agent quality DONE: agents/TASTE.md constitution = my context made portable, self-verify frames rule, one-concern orders; T2 data engine next: analytics activation + experiment discipline + portal analytics tab; T3 owner footage; T4 nightly autopilot). Q1 answered: sharing supervisor context = TASTE.md; agents inherit taste but still need look-judge-redo loop.

**Channel voice (2026-07-22): en-IN-NeerjaExpressiveNeural** (deadpan persona, rate -5%) for drafts AND finals — owner-picked after auditioning ElevenLabs (Ruhaan/Anika pro voices = paid-gated 402 on free tier; 4400 chars left) + free edge Indian roster. pipeline.approve no longer forces elevenlabs. Anika A/B queued for paid tier. TASTE.md/voice lesson: draft voice must equal final voice.

**TTS cache incident (2026-07-22): voice swap was a silent no-op** — cache key used .voice_id (11labs-only) so edge renders keyed identically per persona; rate/pitch also missing. Key now engine|voice|rate|pitch|persona|text. Neerja pacing: +3% rate fits 112w/62s. Creta finally voiced in NeerjaExpressive for real. Lesson: verify by artifact (cache mtimes/audio), not by config.
